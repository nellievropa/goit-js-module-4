const numbers = [5, 15, 25, 36, 47];

numbers.forEach(function (number, index, array) {
    console.log('number', number);

    // щоб додати елемент

    // array[index] = 20;
});

console.log(numbers);
